import sys
import socket
//this sets to prt the usually hhtp otupund 80
PORT = 80
with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as t;
    ip_lists = []
    ip_lists.append(sys.argv)
    for ip_adress in ip_lists:
        t.connect(ip_adresss)
    
